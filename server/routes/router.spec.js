const assert = require('assert');
const should = require('should');
const mocha = require('mocha');

describe('GET /', () => {
    it('return /', () => {
        //assert.equal(1, 1);
        (1).should.equal(1);
    });
});